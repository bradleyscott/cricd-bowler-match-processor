{
  "name": "cricd-bowling-processor",
  "private": true,
  "version": "0.0.0",
  "description": "An HTTP API that can tell you the bowling stats of an innings",
  "main": "index.js",
  "scripts": {
    "debug": "DEBUG=* EVENTSTORE_HOST=localhost node --inspect index.js",
    "debug-windows": "SET EVENTSTORE_HOST=localhost&& SET DEBUG=*&& node --inspect index.js",
    "start": "node index.js",
    "test": "mocha --reporter=spec"
  },
  "license": "ISC",
  "dependencies": {
    "debug": "^2.2.0",
    "express": "^4.13.4",
    "ges-client": "^0.10.0",
    "underscore": "^1.8.3"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/cricd/bowling-processor.git"
  },
  "author": "Bradley Scott",
  "devDependencies": {
    "mocha": "^3.0.2",
    "should": "^11.1.0"
  }
}
